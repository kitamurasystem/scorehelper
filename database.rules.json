{
  "rules": {
    "uploads": {
      ".indexOn": [ "parsedAt", "uploadedAt" ],
      "$sessionId": {
        "$order": {
          ".read": "request.auth != null",
          ".write": "request.auth != null"
        }
      }
    }
  }
}
